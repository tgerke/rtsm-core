name: Release

# Tagging vX.Y.Z publishes versioned container images to GHCR and attaches
# the validation pack (traceability matrix joined to this tag's test
# results — P11-05) to the GitHub release.
on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  packages: write

jobs:
  validation-pack:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: rtsm
          POSTGRES_PASSWORD: rtsm-dev-only
          POSTGRES_DB: rtsm
        ports:
          - 5435:5432
        options: >-
          --health-cmd "pg_isready -U rtsm -d rtsm"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      # The suite derives a dedicated rtsm_test database from this URL and
      # creates + migrates it in the setup step (apps/api/vitest.config.ts).
      DATABASE_URL: postgres://rtsm:rtsm-dev-only@localhost:5435/rtsm
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Generate validation pack
        run: pnpm validation-pack
      - name: Bundle
        run: |
          cp docs/regulatory-traceability.md validation-pack/
          cd validation-pack && zip -r ../validation-pack-${{ github.ref_name }}.zip .
      - uses: actions/upload-artifact@v4
        with:
          name: validation-pack-${{ github.ref_name }}
          path: validation-pack-${{ github.ref_name }}.zip
      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: validation-pack-${{ github.ref_name }}.zip

  images:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: api
            dockerfile: apps/api/Dockerfile
          - name: web
            dockerfile: apps/web/Dockerfile
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}-${{ matrix.name }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,scope=${{ matrix.name }},mode=max
